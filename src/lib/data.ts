
import type { Department, Event, User, Winner } from "@/lib/types";

// NOTE: This file contains mock data for initial setup.
// It is now being replaced by live API calls.
// This data can be safely removed or kept for testing purposes.

export const departments: Department[] = [];
export const users: User[] = [];
export const events: Event[] = [];
export const winners: Winner[] = [];

export const initialDb = {
    departments,
    users,
    events,
    winners
}

    